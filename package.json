{
  "name": "us-command-ultra",
  "version": "14.0.0",
  "description": "Risk-engineered US stock portfolio and market-theme intelligence command center for Render",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.14.0 <25"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node --check server.js && node --check market-data.js && node --check technical-signals.js && node --check astra-bridge.js",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test",
    "build:astra": "npm --prefix frontend run build",
    "test:ui": "npm --prefix frontend test",
    "start:astra": "python run_astra.py"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
